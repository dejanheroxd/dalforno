---
const { foodName, description, price } = Astro.props;
---

<div class="flex flex-col justify-center text-center items-center w-72 mb-28">
  <p class="font-serif text-2xl pb-4">{foodName}</p>
  <p class="pb-3 text-orange-950">
    {description}
  </p>
  <p class="text-xl">${price}</p>
</div>
